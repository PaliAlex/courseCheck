import React from "react";
import { HeaderContainer, LogoWrapper } from "./styles";

export const Header: React.FC = () => (
    <HeaderContainer>
        <LogoWrapper>
            LOGO
        </LogoWrapper>
    </HeaderContainer>
)
